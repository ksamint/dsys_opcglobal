Immediate on/off setting (notifications, public profile). Use a Checkbox when the change needs a submit.

```jsx
<Switch label="Email me new standard drafts" defaultChecked />
<Switch label="Public member profile" description="Visible in the alliance directory." labelPosition="start" />
```
