Labelled text field for forms (membership application, search, contact).

```jsx
<Input label="Organisation" placeholder="Institution or company name" required />
<Input label="Work email" type="email" hint="We reply within five working days." leading={<Icon name="mail" />} />
<Input label="Member ID" error="Enter an ID in the form OPC-000000." defaultValue="OPC-12" />
<Input size="sm" placeholder="Search standards" leading={<Icon name="search" size="sm" />} />
```
